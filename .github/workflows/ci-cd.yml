name: CI / CD

on:
  push:
    branches: [dev, main]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}

jobs:

  # ── 1. CI ────────────────────────────────────────────────────────────────
  test:
    name: CI - Lint and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install pytest pytest-asyncio httpx ruff

      - name: Lint with ruff
        run: ruff check backend/

      - name: Run tests
        run: pytest backend/tests/ -v

  # ── 2. CD - Build and Push ────────────────────────────────────────────────
  build-and-push:
    name: CD - Build and Push Images
    runs-on: ubuntu-latest
    needs: [test]

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: ${{ env.IMAGE_PREFIX }}-frontend:latest

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ${{ env.IMAGE_PREFIX }}-backend:latest

  # ── 3. CD - Deploy to ECS ─────────────────────────────────────────────────
  # Uncomment this job if deploying to AWS ECS.
  # Requires secrets: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY
  #
  # deploy-ecs:
  #   name: CD - Deploy to ECS
  #   runs-on: ubuntu-latest
  #   needs: [build-and-push]
  #
  #   steps:
  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: us-east-1
  #
  #     - name: Deploy frontend to ECS
  #       run: |
  #         aws ecs update-service \
  #           --cluster docker-compose-app-cluster \
  #           --service docker-compose-app-frontend \
  #           --force-new-deployment
  #
  #     - name: Deploy backend to ECS
  #       run: |
  #         aws ecs update-service \
  #           --cluster docker-compose-app-cluster \
  #           --service docker-compose-app-backend \
  #           --force-new-deployment

  # ── 4. CD - Deploy to EKS ─────────────────────────────────────────────────
  # Uncomment this job if deploying to AWS EKS.
  # Requires secrets: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY
  # Requires: EKS cluster already provisioned via terraform-eks/ and setup-eks.sh
  #
  # deploy-eks:
  #   name: CD - Deploy to EKS
  #   runs-on: ubuntu-latest
  #   needs: [build-and-push]
  #
  #   steps:
  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: us-east-1
  #
  #     - name: Install kubectl
  #       uses: azure/setup-kubectl@v3
  #
  #     - name: Update kubeconfig
  #       run: aws eks update-kubeconfig --region us-east-1 --name docker-compose-app-cluster
  #
  #     - name: Restart deployments to pull latest images
  #       run: |
  #         kubectl rollout restart deployment/frontend -n app
  #         kubectl rollout restart deployment/backend -n app
